# HANDOFF

最終更新: 2026-08-31

## いま何をしているか

**外部リンク0件の解消に着手したところ。** その1段目として、運営者情報ページ `/about/` を新規作成した。

外部リンクは「もらいに行く」のではなく「もらえる状態を作ってから、載る資格のある場所に申し出る」
順序でしか安全に増やせない。段階は次のとおりで、いま段階1にいる。

| 段階 | 内容 | 状態 |
|---|---|---|
| 1. 資格を作る | 運営者・連絡先・出典方針を明示する | **実装済み・検証中** |
| 2. 手元の1本 | 姉妹サイト（うちなーマネー）からのリンク | **調査中** |
| 3. 申し出る | 自治体・観光協会へ掲載依頼（送信は要許可） | 未着手 |
| 4. 引かれる | 出典として引用される品質のデータを積む | 継続 |

**やらないと決めたこと**: 有料リンク、リンクファーム、相互リンク集、大量のディレクトリ一括登録。
Google のスパムポリシー違反で、積み上げた土台ごと失うリスクがある。

### いま動いているエージェント（このセッション終了時に未完なら、結果は失われる）

- `seo-analyst` … GSC で姉妹サイトの実体と被リンク状況を調査中（読み取りのみ）
- `verifier` … `/about/` の実装を独立検証中

## 今回やったこと（未コミット）

`frontend-dev` が実装。**以下はエージェントの自己申告であり、`verifier` の判定は未取得。**

- `src/app/about/page.tsx`（新規） … `privacy/page.tsx` と同じ構造・スタイル。
  h1「このサイトについて」→ h2 4つ（運営者 / 掲載情報の集め方 / 情報の誤りを見つけたら / 広告について）。
  連絡先 `info@nexeed-lab.com` を `mailto:` リンクで2箇所
- `src/app/sitemap.ts` … `staticRoutes` に `/about/` を追加（priority 0.3 / yearly）
- `src/app/layout.tsx` … フッターに `/about/` リンクを追加

`git status`: `M src/app/layout.tsx` / `M src/app/sitemap.ts` / `?? src/app/about/`

### 本文に書いてよい事実（これ以外を足さない）

運営者名「Nexeed Lab」、メール `info@nexeed-lab.com`、出典の種類（各施設の公式サイト／
沖縄県バリアフリーマップ／市町村・観光協会の公式サイト）、AdSense の記載、
プライバシーポリシーへの内部リンク。**所在地・電話番号・設立年は創作しない。**

## このセッションで本番に入ったもの（検証済み）

| コミット | 内容 |
|---|---|
| `cb5f7f5` | 内部リンクの孤児化を解消（トップのディレクトリ型リンク 0→26本）、sitemap 156→116、CTRの低い8ページのメタ文言を個別化 |
| `f7eb427` | 専門エージェント5体を `.claude/agents/` に定義 |
| `da71fe8` | 恩納村を `CITIES` に追加、万座毛を一次情報で検証して登録（85→86件） |
| `b9da3bc` / `615e3b7` / `88e7e03` / `0bf8312` / `bcc32ac` | 本番検証結果とインデックス登録リクエストの記録 |

すべて `origin/main` に push 済み。本番HTMLを curl して反映を確認済み。

### GSC の実測ベースライン（4週間後の答え合わせに使う。消さないこと）

プロパティ `sc-domain:uchina-map.nexeed-lab.com`、期間 2026/05/29〜08/28（90日）。

- 全体: クリック139 / 表示5,170 / CTR 2.7% / 平均掲載順位 9.6
- 上位2ページ（`naha-airport-kids` 表示2,331、`naha-main-place` 表示557）で全クリックの55%
- インデックス: 登録済み94 / 未登録121（検出-インデックス未登録 **79**、代替ページ23、リダイレクト17、クロール済み-未登録2）
- **外部リンク 合計 0 件** / 内部リンク 11件
- 表示はあるがクリック0のクエリ: 浦添大公園 駐車場 157、おきなわワールド 駐車場 49、
  メインプレイス 授乳室 40、パルコシティ 授乳室 29、おきみゅー 駐車場 21
- 絞り込みページ66枚の合計: 26表示・0クリック（うち市町村×設備は12表示・0クリック）
- スポット詳細ページの表示回数の**中央値は90日で15**

### メタ文言を個別化した8ページ（変更前の実測値つき）

`urasoe-parco-city`(390表示6クリック) / `urasoe-daikoen`(233/1) / `nanjo-okinawa-world`(187/2) /
`naha-okimu`(168/0) / `naha-shuri-castle-park`(132/3) / `tomi-dmm-aquarium`(92/1) /
`ginowan-harmony-chafe`(84/2) / `naha-mori-no-ie-minmin`(74/0)

選定基準は「表示回数60以上 かつ CTR 3%未満」。

### インデックス登録リクエスト（2026-08-31 に全件送信済み）

**「検出 - インデックス未登録」だったスポット詳細29ページ + 万座毛の計30ページ、
すべて「インデックス登録をリクエスト済み」を画面で確認した。** 同日で計32回送信（重複2回）。
クォータ上限には当たらなかった。

`kitanakagusuku-aeon-rycom` は送信の数分後に再検査したところ
**「URL は Google に登録されています／ページはインデックスに登録済みです」に変化していた**。

絞り込みページ50件は、sitemap から外した市町村×設備40件と重なるため意図的に送っていない。

## 発見した再利用可能な出典（重要）

**沖縄県バリアフリーマップ** `http://okinawa-bf-map.jp/facility-info/detail?facility_id=<ID>`

沖縄県の公的サイト。授乳室・ベビーベッド・多目的トイレ・ベビーカー貸出・哺乳瓶の洗い場・
ミルクのお湯提供・キッズスペース・駐車場台数・段差・エレベーター基数・通路幅が
**構造化されて最終更新日つきで載っている**。緯度経度も取れる。

**HTTPS で証明書のホスト名が一致しないため `WebFetch` は必ず失敗する。`curl` で `http://` のまま取る。**
施設IDは `WebSearch` を `allowed_domains: ["okinawa-bf-map.jp"]` で絞って探す。
手順は `.claude/agents/spot-data-curator.md` に記載済み。

施設の公式サイト（`manzamo.jp` / `onnanoeki.com`）には子連れ設備の記載が**一切無かった**。
公式サイトだけ見て「記載なし＝設備が無い」と判断しないこと。

## 内部リンク診断の裏付け（URL検査で得た事実）

URL検査の「検出」欄に出た参照元ページ:

- `motobu-kaiyohaku-park` … `/spots/?age=0`
- `nago-fruitsland` … `/spots/?feature=rainOk`
- 他8件 … **検出されませんでした**（うち3件は参照元サイトマップすら未検出）

Google が認識できていた参照元は2件だけで、どちらもクエリ形式URL（canonical は `/spots/`）。
2026-08-31 に修正した内部リンクの孤児化が実在したことの、Google 側からの裏付けになる。
**次回GSCを見るときは、この参照元ページ欄がディレクトリ型URLに変わっているかを観測点にする。**

## 未検証のもの（推測であって事実ではない）

- **`/about/` の実装は `verifier` の判定を取得していない。** `frontend-dev` の自己申告のみ
- **`/about/` はデプロイしていない。** 本番は変更前のまま
- 姉妹サイト「うちなーマネー」の URL・規模・uchina-map へのリンクの有無は**未取得**（`seo-analyst` が調査中）
- 今回の一連の変更で流入が増えるかは未検証。効果はGSCで実測するまで分からない
- `parkingFree` を明示していないスポットが3件あり、デフォルト値 `true`（無料）が効いている:
  `naha-okimu` / `naha-shintoshin-park` / `urasoe-daikoen`。
  `urasoe-daikoen` は「浦添大公園 駐車場」が157表示の最重要クエリで、
  新タイトルに「駐車場は無料・広め」と書いた。**この無料はデフォルト値であって未確認**
- 今帰仁村は未追加。今帰仁城跡のバリアフリー情報（`facility_id=1495`、授乳室**無**・
  ベビーベッド有・多目的トイレ有・駐車場155台）は取得済みだが、**観覧料の一次情報が未取得**
  （`nakijinjoseki.jp` が接続タイムアウト）
- 公的ポータルの見込み: 沖縄県「こどもミライ」（こども未来部こども家庭課）を確認したが、
  **民間サイトへのリンク集も掲載依頼の仕組みも無かった**。県レベルの公式ポータルは見込み薄

## 保留中の判断

**モバイル幅でフッターが表示されない**（`hidden md:block`）ため、`/about/` へのリンクが
スマホでは出ない。既存のプライバシーポリシーリンクと同じ扱いなので指示どおりではある。

- 外部リンクを増やす目的（クローラーと、掲載を検討する組織が運営者を確認できること）には**足りている**
- 「設備情報の誤りを知らせてもらう」目的には**効かない**。利用者はほぼスマホのはず

全ページのフッター表示に関わる設計判断なので、ユーザーに諮ってから決める。

## 次にやること

1. `verifier` の判定を受け取り、合格なら `/about/` をコミットして `main` に push（デプロイは要確認）。
   不合格なら指摘を `frontend-dev` に戻す
2. `seo-analyst` の結果を受け取り、姉妹サイトからのリンクを張るか判断する
   （サブドメイン同士なので効果は限定的である点を踏まえて評価する）
3. モバイルのフッター問題をユーザーと決める
4. 段階3（自治体・観光協会への掲載依頼）。**メール送信は必ず事前に許可を取る**。
   狙い目は市町村の観光協会（例: 恩納村観光協会 `goto-onna.com`）。県の公式ポータルは見込み薄
5. **2〜7日後**: GSC のインデックス登録レポートで「検出 - インデックス未登録 79件」が減ったか確認
6. **2026-09-28以降**: CTR実験（上記8ページ）とクロール実験（内部リンク+sitemap）の答え合わせ

## 触ってはいけないところ

- **成績の良いページ**: `naha-main-place`(CTR 7.7%) / `ginowan-tropical-beach`(8.1%) /
  `tomi-toyosaki-beach`(12.5%) / `chatan-araha-park`(25%)。`seoTitle` を設定しない限り
  従来のテンプレートが使われる設計なので、放置でよい
- `src/lib/seo.ts` の `pageMetadata()` は全ページが通る共通関数。`titleAbsolute` の既定値 false を
  変えると全ページのタイトルが壊れる
- `src/app/sitemap.ts` の市町村×設備40件の除外は**意図的**。ページを消したわけではないので、
  「ルートがあるのに sitemap に無い」のを不整合とみなして戻さないこと
- `public/_headers` / `next.config.ts` の `output: "export"` / `trailingSlash: true` /
  `images.unoptimized: true`
- スポットの事実（階・営業時間・料金・設備）は `src/data/spots.ts` が唯一の出所。
  メタ文言に書く数字はここに無ければ書かない
- ワークツリーで作業中。**stash スタックは本体と共有**なので素の `git stash` を使わない

## デプロイ手順

- Cloudflare Workers の静的アセット配信。`wrangler.jsonc` が `assets.directory: "./out"` を指定
- **`main` への push で Cloudflare Workers Builds が走る**（反映まで数分）
- `git push origin HEAD:main` はこの環境で動作する（CLAUDE.md の GitHub MCP 経由は
  git が403を返す環境向けの記述。403が返った場合のみMCPに切り替える）
- デプロイ後は必ず本番HTMLを curl して反映を確認する:

```bash
curl -s -o /dev/null -w "%{http_code}\n" https://uchina-map.nexeed-lab.com/about/
curl -s https://uchina-map.nexeed-lab.com/sitemap.xml | grep -c '<loc>'
curl -s https://uchina-map.nexeed-lab.com/about/ | grep -o 'mailto:info@nexeed-lab.com'
```
